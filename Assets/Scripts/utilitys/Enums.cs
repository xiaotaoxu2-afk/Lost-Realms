public enum EnemyState
{
    patrol,
    chase,
    skill,
    death,
    hurt,
    attack1,
    attack2,
    attack3
}

public enum SceneType
{
    Loading,
    Menu
}

public enum PersistentType
{
    ReadWrite,
    DoNotPersist
}
